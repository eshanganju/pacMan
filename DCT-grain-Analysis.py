"""This code reads the grains and analyzes them for overlap, 2D morphlogy, and grain boundary correspondence
"""


import numpy as np
import tifffile as tf
from glob import glob

from pac import Measure


# Input and output locations

# Grain index and values

# Loop over grain number:
	
	# Read solid grains from DCTs and SEM
	# Read grain boundaries from DCT and SEM data 
	
	# Compute grain shape statistics
	# Compute grain IOU
	# Compute grain boundary distances

# Save the outputfile